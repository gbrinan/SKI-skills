# Progress Log

> **각 단계를 완료하거나 문제가 발생하면 업데이트하세요.**

## Session 2026-08-24

### Phase 1: Requirements & Discovery ✅

**작업 내역**:

1. 사용자 요청 분석: 스킬 2개(모더레이터 코치 / 와이어프레임 코치), 입력=md 5종, 철학 2종 채택
2. 기존 스킬 분석: AgentTaskFit_v3.0.md, WireframeCoach_v8.md
3. 디자인캠프 산출물 5종 분석 (세션 중 4·5번 추가 업로드 반영)
4. 참조 저장소 철학 조사 (ahastudio/file-based-planning-workflow, LilMGenius/paperthin)
5. spec.md 작성

### Phase 2: Planning & Structure ✅

**작업 내역**:

1. plan.md 작성 — 계승/폐기 요소 결정, `@@STATE@@` 연결 설계
2. findings.md에 기술적 결정 8건, 이슈 2건 기록

### Phase 3: Implementation ✅

**작업 내역**:

1. `skills/wireframe-moderator-coach.md` 작성 (v1.0) — Lv4 지도 → 5기준 워크시트 HTML → 6게이트 판정 → 상태 블록
2. `skills/wireframe-coach.md` 작성 (v1.0) — 리부트 체크 → 판단기준 캐기 → 환경/방식 판정 → 스킬 2~3개 추천

**생성/수정 파일**:

- `skills/wireframe-moderator-coach.md` (새로 생성)
- `skills/wireframe-coach.md` (새로 생성)
- `docs/features/wireframe-coach-suite/` 6종 (새로 생성)

### Phase 4: Testing ✅

**작업 내역**:

1. 산출물 계약 자체 검증: 프론트매터 2종, 판정/게이트 어휘 3종 고정, 상태 블록 필드 수(12/11) 일치, 스킬 1 출력의 `@@STATE@@`를 스킬 2가 그대로 읽는 왕복 확인
2. 커밋 및 `claude/file-based-skill-design-74360t` 브랜치 푸시

### Error Log

| 에러 | 한 줄 요약 |
|---|---|
| 디자인캠프 4·5 최초 누락 | 세션 중 추가 업로드로 해소, 5종 전체 반영 |

## 5-Question Reboot Check (다음 세션용)

1. **현재 어느 단계인가?** Phase 5 완료 — 모더레이터 코치 v1.4의 6페이지 선정 보드·검증기·배포본 완성
2. **다음에 할 일은?** 실제 워크샵에서 v1.4로 파일럿 운영하고, 허용된 브라우저 환경에서 375·768·1280 화면을 직접 확인해 findings.md에 기록
3. **목표는?** 디자인캠프 md 5종 → Lv5 선정 → 와이어프레임 → 스킬 추천까지 이어지는 코치 파이프라인
4. **지금까지 배운 것은?** findings.md 참조 (레벨 명칭 불일치 대응, 상태 블록 연결, 계승/폐기 결정)
5. **완료한 작업은?** 문서 정본 갱신, 스킬 2종, 모더레이터 v1.4 템플릿·렌더러·validator·가상 예시·배포본 작성

## Session 2026-08-24 (2차)

### 실전 구동 + 예시 팩 확장 ✅

**작업 내역**:

1. 모더레이터 코치 실전 구동 (회계관리팀 md 5종): Lv4 지도 13개 → Lv5 후보 14개 × 5기준 워크시트 HTML 산출 (판정 전 — 팀의 Lv5 선택 대기)
2. 사용자 요청으로 도메인 예시 팩 3종 추가: 인사팀 / 경영전략팀 / 운영혁신팀 (`examples/design-camp/`)
   — 회계관리팀 실물 산출물의 파이프라인 구조(업무 트리 → Pain Point 및 AI 전환 매핑 → 현재 업무 흐름 → 개선 업무 흐름 → 스킬 구체화)를 따르되 전 항목 [예시] 표기

**생성/수정 파일**:

- `examples/design-camp/인사팀_디자인캠프_통합예시.md` (새로 생성)
- `examples/design-camp/경영전략팀_디자인캠프_통합예시.md` (새로 생성)
- `examples/design-camp/운영혁신팀_디자인캠프_통합예시.md` (새로 생성)

## Session 2026-08-24 (3차) — 전체 파이프라인 리허설 ✅

**작업 내역**:

1. 모더레이터 코치 v1.1 실전 구동: 신호등 선정 트리 HTML (🟢4·🟡3·🔴7 + 제외 11, 회색 처리) → 사업보고서(A-A3-2-1) 선정 → 판정 **조건부 권장** (⑥가치만 미확인)
2. 와이어프레임 코치 실전 구동: To-Be 설계도 11노드 → 판단기준 3곳 대화로 확정
   (필수누락·불일치→형식오류 + 양식 태그 / 요청 후 N일 무응답→독촉, N 커스텀 / 변동 비율 커스텀 플래그, 애매→👤)
3. 스킬명 3개 참가자 확정: 공시자료 취합 체커 / 재무-DART 크로스체커 / 사업보고서 정성항목 초안기
4. @@STATE@@ 블록 왕복(스킬1→스킬2→갱신) 동작 확인

**리허설에서 확인된 것**: 한 턴 질문 1개·보기 제시·해석 되받기·커스텀 값 (미정 아닌 파라미터화) 처리 모두 계약대로 동작.
커스텀 파라미터({N일}, {비율}) 개념은 스킬 문서에 명시돼 있지 않아 v1.1에서 추가 검토 → findings 참조.

## Session 2026-08-24 (4차) — 하류 직결 계약 반영 ✅

1. 모더레이터 코치 v1.2: atf-data JSON 블록 내장 + 검증 규칙 5번 추가
2. 와이어프레임 코치 v1.1: WFDATA md 블록 출력 + 커스텀 파라미터 규칙 + 우선 배지 단일화
3. 근거: jcurve_ski 통합 파이프라인 E2E 검증(3차)에서 확인된 형식 간극·해석 차이 봉합

## Session 2026-08-24 (5차) — 풀테스트 + 세션 간 인수인계 ✅

1. HR(서류전형)·전략기획(동향 브리핑) 도메인 풀테스트: 선정·판정 → To-Be·스킬 3개 → atf-data/WFDATA 생성
   → jcurve_ski 하니스 L0(RED→팀확정→GREEN) → L1~L4 전체 통과 (양쪽 모두)
2. 결과물 저장소 편입: examples/full-test/ (커밋 71f9527, 푸시 완료)
3. 세션 간 인수인계: "팀 Lv.5 에이전트 통합 패키징" 세션(session_0188GNcDjqFkNf1RDFKYTYGf)에
   원샷 트리거(trig_01F7KDefxfqUk2pkLTTirob4, 03:10 UTC 발화)로 결과물 위치·검증 요약 전달

## Session 2026-08-24 (6차) — 배포 검증 + 영업 케이스 ✅

1. `.claude/skills/` 재배치가 실제 동작함을 확인 — 이 세션의 스킬 목록에 두 코치가 자동 등재됨
2. 영업팀(화학 B2B) 예시 팩 추가 (`examples/design-camp/영업팀_디자인캠프_통합예시.md`)
3. 영업 풀테스트: 견적 대응(T-B1-1-1) 선정 → 스킬 3개(RFQ 정리기/마진 검토표 생성기/견적서 초안 생성기)
   → 하니스 L0(RED→GREEN) → L1~L4 전체 통과. 커스텀 파라미터 {마진 기준율}·🚧 ERP 구간 규칙대로 처리

## Session 2026-08-24 (7차) — 운영혁신 풀테스트 + 회고 ✅

1. 운영혁신 풀테스트: 재고 점검(T-O2-2-1) 선정 → 스킬 3개(재고 대사 체커/이상 재고 플래거/재고 브리핑 작성기)
   → 하니스 L0(RED→GREEN) → L1~L4 전체 통과. 커스텀 파라미터 2개({과잉률}·{경과일}) 동시 처리 확인
2. 5개 도메인 완주 회고 작성: retrospective.md — 갭 8건(G1~G8) 식별, 우선순위 부여

## Session 2026-08-24 (8차) — 대화 성격 명문화 + 분기 체인 ✅

1. 모더레이터 코치 v1.3: **부분 대화형** 계약 — 자동 구간(질문 0)과 대화 구간(Lv5 선택·❓칩·⑥가치) 분리,
   "안 물어본 것을 미확인 처리해 판정 금지, 배치에서는 판정 전으로 멈춤" (G1 근본 원인 봉합)
2. 와이어프레임 코치: **완전 대화형** 성격 명문화
3. G3 해소: 분기형 4-스킬 팩(견적 통과/미달 갈림길) → 하니스 "팀 에이전트 (갈림길 있음)" 분류, L1~L4 통과 (끝점 2개 halt 검사)
4. 배포 사본 v1.3으로 동기화

## Session 2026-08-24 (9차) — G2 역할 경계 확정 ✅

jcurve_ski lv5-packaging-prompt-v2 검토 → 팀 확정 인터뷰 역할을 이미 수행(부분 대화형, contract 블록 산출)함을 확인.
새 스킬 만들지 않기로 결정, wireframe-coach 헤더에 하류 연결 명시. 3단 파이프라인 경계 확정.

## Session 2026-08-24 (10차) — G6 해소 ✅

Sonnet 5로 모더레이터 코치 v1.3 실행: 신호등·추림 완전 일치, "판정 전" 정지 준수.
계약 구멍 발견·봉합: 판정 전 HTML에 atf-data 금지 (허용값 밖 verdict의 하류 오염 방지). 예시 팩 헤더 수치 정합.

## Session 2026-08-25 (11차) — Moderator v1.4 UI 완료

1. 회계관리팀 v7 참고 HTML을 시각 기준으로 분석하고 `DESIGN.md`에 따뜻한 종이형 디자인·접근성·반응형 계약을 기록
2. 6페이지 선정 보드 템플릿과 JSON 렌더러 추가: 결과 읽기 → 전체 지도 → LV4 → LV5 → 현재 LV5의 LV6 → 최종 토의
3. 사람 업무를 `사람이 준비/연동 필요/사람이 최종 책임`으로 분리해 자동화 수에서는 제외하고 경계로 보존
4. `PP`·`HITL`·`AX` 같은 화면 약어를 쉬운 표현으로 바꾸고 validator에서 단독 노출을 차단
5. 가상 경영전략팀 예시 생성, 판정 전/완료·사람 전용 후보·약어·브라우저 런타임 문법·공격성 JSON 계약 검증 51개 통과
6. 독립 검토에서 발견한 수치·역할 속성 삽입을 수정: 렌더러 입력 스키마, 모든 동적 속성 이스케이프, 불완전 ATF 거부
7. LV4·LV5 페이지 이동과 역할 필터 재렌더 뒤 키보드 포커스를 새 컨트롤로 복원
8. 이노허브 구버전 v1.3 사본 제거, 동기화 스크립트에 stale 사본 정리 추가
9. 렌더러와 사후 HTML validator의 입력 검증을 `moderator-schema.mjs`로 단일화하고, 수동 변조 7종을 회귀 테스트에 추가
10. 선택 LV5/LV6와 ATF 대상·엔진을 의미적으로 결속하고, JSON 블록 분기·중복·필수 패널 ID 누락 회귀 테스트를 추가
11. script 속성 순서·따옴표 형태와 무관하게 같은 ID의 기계 블록을 모두 찾아 중복 우회를 차단
12. HTML 숫자 문자 참조로 숨긴 ID를 디코딩하고 `</scripture>` 같은 잘못된 닫는 태그를 거부
13. HTML 오류 복구에서 유효한 `<script/id=...>` 형태까지 탐지해 중복 기계 블록 우회를 차단
14. `<scripture>`를 실제 script로 오인해 화면 약어를 숨기던 검사를 수정하고, 스킬·README·spec의 비IT 용어와 6페이지 계약을 정본화
15. 근거 없는 후보를 스키마에서 차단하고 팀 재검토 이유를 화면에 표시하며, 문자 참조로 숨긴 약어도 검사하도록 보강
16. 5개 선정 기준과 신호등 상태를 결속하고 주석 속 가짜 패널 ID를 거부하며, 조사 문서의 약어를 쉬운 용어로 교체

### Error Log

| 에러 | 원인 | 해결 |
|---|---|---|
| 약어 부정 테스트가 처음 통과 | 테스트가 화면이 아니라 실행 스크립트 안 문자열을 바꿈 | 실제 정적 화면 문구를 바꾸도록 fixture 수정 후 validator 실패 확인 |
| 로컬 HTML 브라우저 열기 차단 | 현재 앱 브라우저 정책에서 로컬 파일 URL을 허용하지 않음 | 구조·행동 validator를 먼저 완료하고 실제 브라우저 시각 QA는 허용된 표면에서 별도 수행 |
| 공식 `quick_validate.py` 실행 실패 | 번들 Python에 PyYAML 모듈이 없음 | 프론트매터 구조·SSOT 사본 일치를 별도 검사, 의존성 추가 없이 실패 사실 보존 |
| 보안 검토가 수치·역할 마크업 삽입 가능성을 발견 | 렌더러가 타입·enum을 확인하지 않고 일부 값을 `innerHTML`에 넣음 | 입력 경계 스키마 검증, 방어적 이스케이프, 공격성 fixture 6개 추가 |
| 사후 validator가 렌더러보다 느슨한 타입을 허용 | 생성 후 HTML의 JSON을 손으로 바꾸면 문자열 불리언·음수 번호·빈 엔진명이 통과 | 공통 스키마 모듈을 양쪽에서 호출하고 변조 fixture 7종으로 재발 방지 |
| Windows에서 배포 동기화 스크립트 실행 실패 | 번들 Git Bash의 PATH에 `dirname`·`cp` 등 coreutils가 없음 | 정본과 두 사본에 동일 패치를 적용하고 줄 단위 비교 0건으로 동기화 확인 |

## Session 2026-08-25 (12차) — Moderator 단계 탐색 파일럿

1. 별도 작업 `01a0374b-7985-7490-a62d-e0901c7e3670`의 요구사항을 현재 6페이지 보드와 대조
2. 전체 LV4·전체 LV5·원본 AI 전환 추천·모더레이터 기준 충족의 4단계 요약과 LV5 보기 필터 구현
3. 원본 추천 `sourceRecommendation`과 모더레이터 판단 `eligible`을 분리하고 가상 경영전략팀 5개 LV5에 적용
4. `후보 제외`·취소선을 중립 상태 언어로 교체하고 원본 업무·사람 업무를 계속 보존
5. 판정 완료에 팀 확인·선택 이유를 필수화하고, 기준 미충족 업무 선택 시 예외 선택 이유를 강제
6. 실행되지 않는 `<template>` 내부의 가짜 페이지가 validator를 속이지 못하도록 보강
7. 렌더·validator·공격성 fixture를 포함한 61개 계약 검증과 Node 문법 검사 통과
8. 새 에이전트가 스킬 정본과 가상 JSON만 받아 독립 실행: 6페이지 렌더·validator 통과, LV4 3·LV5 5·원본 추천 2·모더레이터 기준 충족 3·팀 확정 선택 1을 구분

### Error Log 추가

| 에러 | 원인 | 해결 |
|---|---|---|
| Edge·Chrome headless 캡처 3회 실패 | 이 Windows 환경에서 GPU 프로세스가 `-1073741790`으로 반복 종료 | 구조·런타임·반응형 CSS 계약은 검증했으며, 375/768/1280 실제 캡처는 브라우저가 정상 동작하는 환경에서 재실행할 미완료 게이트로 유지 |

## Session 2026-08-25 (13차) — Wireframe Coach LV5 전체 파일럿

1. 사용자 확인으로 최신 계약을 단일 LV6가 아닌 `LV5 전체 지도 → 스킬 2~3개 → 스킬별 템플릿 채움 힌트`로 확정
2. 가상 시장 동향 브리핑 6개 LV6를 10개 본선 노드로 연결하고, 사람 준비·애매 기사 검토·최종 확정을 자동화 밖 경계로 보존
3. `LV5MAP` 1개, `SKILLHINT` 3개, `WFDATA` 3개와 상태 블록을 포함한 최종 MD 생성
4. 누락 지도, 힌트·WFDATA 수 불일치, 지도 밖 노드, 실행 환경 선확정, 추천 수 오류를 거부하는 회귀 테스트 7개 통과
5. 자체완결 4페이지 HTML 생성: 결과 요약, LV5 전체 흐름, 추천 스킬, 템플릿 힌트
6. 앱 브라우저에서 375·768·1280 실제 탭 이동, 가로 넘침 0, 키보드 오른쪽 화살표 이동과 포커스 동기화 확인
7. 새 에이전트가 스킬 정본만 보고 만든 별도 결과를 validator로 검증해 추천 2개·힌트 2개·WFDATA 2개 계약 통과
8. 12개 화면군의 56개 최신 구간 캡처를 다시 생성해 세로 누락 0, 가로 넘침 0, 탭 목록 1개, 전체 지도 경로 10개를 확인
9. 모바일 한글 단어 중간 분리 방지, 전체 흐름의 다음·예외 경로 표시, 접근 가능한 4페이지 탭 구조를 최종 HTML에 반영

### Error Log 추가

| 에러 | 원인 | 해결 |
|---|---|---|
| `file://` 브라우저 탐색 차단 | 앱 브라우저 URL 보안 정책 | 127.0.0.1 로컬 전용 서버에서 동일 자체완결 HTML을 검증 |
| 긴 페이지 전체 캡처가 가로로 찌그러짐 | 앱 브라우저의 full-page 캡처 합성 결함 | 제품 DOM 폭은 정상임을 확인하고 실제 뷰포트 구간 캡처를 별도 증거로 사용 |

## Session 2026-08-25 (14차) — 비개발자 화면 언어와 세 코치 명칭

1. 화면 이름을 `디자인 캠프 해설 코치 → 에이전트 설계 코치 → 스킬·에이전트 통합 코치`로 정리
2. 각 HTML의 용도를 LV5 선택, LV5 전체 작업 재설계, 팀 스킬 통합·목업 직접 테스트·발표자료로 명시
3. 기계 판독용 `nodes` 필드는 유지하되 화면과 대화에서는 `작업`, `작업 단계`, `작업 수`로 번역
4. LV5 전체 흐름에 10개 작업과 10개 다음·예외 경로를 데이터에서 그려 연결 관계를 표시
5. 375·1280 실제 브라우저에서 두 HTML의 새 이름·용도·가로 넘침 없음 확인
